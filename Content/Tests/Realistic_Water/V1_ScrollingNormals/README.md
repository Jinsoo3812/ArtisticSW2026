# V1 — Scrolling Normals

This folder preserves the accepted pre-flow result before V2. V1 uses three
world-space normal maps with independent scrolling motion, distance Mip bias,
controlled roughness, and a texture-driven glint mask.

The assets are independent material-instance, waves, and test-level snapshots.
The root-level assets remain compatibility aliases and may point to newer work.
