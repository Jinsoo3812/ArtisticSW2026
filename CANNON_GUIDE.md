# 대포(Cannon) & 선박 GAS 통합 에디터 세팅 및 테스트 가이드

이 문서는 C++ 코드가 빌드된 후 언리얼 에디터 상에서 설정해야 하는 필수 에셋 세팅 방법과 정상 작동 여부를 테스트하는 절차를 한글로 정리한 가이드입니다.

---

## 1. 에디터 에셋 세팅 가이드 (Editor Settings)

### A. 대포알 블루프린트 생성 및 세팅 (`BP_Cannonball`)
1. **블루프린트 생성**: `ACannonball` C++ 클래스를 상속받는 블루프린트 `BP_Cannonball`을 생성합니다.
2. **메쉬 지정**: `CannonballMesh` 컴포넌트에 알맞은 구체(Sphere) 메쉬를 지정하고 스케일을 조정합니다.
3. **Gameplay Effect(GE) 생성 및 할당**:
   - 콘텐츠 브라우저에서 `GameplayEffect`를 상속받는 블루프린트 `GE_ShipDamage`를 생성합니다.
   - `GE_ShipDamage` 설정:
     - **Duration Policy**: `Instant`
     - **Modifiers**: 항목을 하나 추가하고 다음과 같이 세팅합니다.
       - **Attribute**: `BaseAttributeSet.Damage` (GASCore에 정의된 메타 데미지 어트리뷰트)
       - **Modifier Op**: `Add`
       - **Magnitude Calculation Type**: `Set By Caller`
       - **Data Tag**: `Data.Damage` (만약 해당 태그가 없다면, 단순히 `Override`나 `Add` 타입의 `Scalable Float`로 지정하여 고정 데미지 10을 주셔도 테스트 가능합니다. C++ 코드에서 동적으로 이 태그를 통해 선박의 공격력 값을 전달합니다.)
   - 생성한 `GE_ShipDamage`를 `BP_Cannonball`의 디테일 패널 내 **Damage GE Class** 속성에 할당합니다.
4. **기타 속성**: 
   - **Life Time After Water Hit**: 대포알이 물에 닿아 비주얼과 콜리전이 꺼진 후 액터가 완전히 파괴될 때까지의 대기 시간(초)입니다. (기본값: 2.0초)
   - **Ripple Amplitude**: 물에 닿을 때 생성될 파동의 강도입니다. (기본값: 50.0)

---

### B. 대포 블루프린트 생성 및 세팅 (`BP_Cannon`)
1. **블루프린트 생성**: `ACannon` C++ 클래스를 상속받는 블루프린트 `BP_Cannon`을 생성합니다.
2. **메쉬 지정**:
   - `BaseMesh` 컴포넌트(포대, 좌우 회전 담당)에 대포 받침대 메쉬를 지정합니다. (테스트용 Cube 등)
   - `BarrelMesh` 컴포넌트(포신, 상하 회전 담당)에 포신 메쉬를 지정합니다.
3. **카메라 배치**:
   - `AimCamera` 컴포넌트는 `BarrelMesh` 아래에 자동으로 부착되어 있습니다.
   - 에디터 상에서 `AimCamera`를 선택하고 상대적 위치/회전(Relative Transform)을 조절하여 **1인칭 조준 뷰** 혹은 포신 바로 뒤쪽에서 조준하기 편한 시점이 되도록 커스텀합니다.
4. **조준 UI 및 입력 에셋 할당**:
   - **Aim Widget Class**: 대포 조준 시 화면에 띄울 크로스헤어 등이 그려진 Widget Blueprint를 만들어 할당합니다. (없다면 간단한 십자선 위젯을 새로 생성해 지정하세요.)
   - **Cannon Input Mapping Context**: 대포 전용 조작을 위해 작성된 IMC(Input Mapping Context)를 지정합니다.
   - **Cannon Look Action**: 마우스 조준 입력용 Action을 지정합니다. (Value Type: Axis2D)
   - **Cannon Fire Action**: 발사 입력용 Action을 지정합니다. (좌클릭 매핑)
   - **Cannon Exit Action**: 조준 모드 해제 및 하차 입력용 Action을 지정합니다. (F 키 매핑)
5. **투사체 설정**:
   - **Cannonball Class**: 위에서 만든 `BP_Cannonball` 블루프린트 클래스를 지정합니다.
   - **Fire Velocity**: 발사 시 대포알의 속도입니다. (기본값: 3000)
   - **Fire Cooldown**: 대포 발사 재장전 대기 시간입니다. (기본값: 1.5초)

---

### C. 선박 블루프린트 세팅 (`BP_Ship`)
1. **대포 장착**:
   - `BP_Ship` 블루프린트(또는 레벨 상에 배치된 선박)를 열고, 대포를 배치할 위치에 `Child Actor Component`를 추가합니다.
   - 추가한 컴포넌트의 **Child Actor Class**에 `BP_Cannon`을 지정합니다.
   - 또는 런타임에 선박의 소켓 등에 `BP_Cannon`을 스폰하여 부착하셔도 됩니다.
2. **GAS 디버그 확인 환경 준비**:
   - 선박이 스폰되면 C++ `BeginPlay`에서 자동으로 GAS AbilitySystemComponent와 AttributeSet이 초기화됩니다.
   - 초기값은 **체력(Health): 100, 최대 체력(MaxHealth): 100, 대포 데미지(AttackPower): 10, 이동속도 계수: 1.0**입니다.

---

## 2. 기능 테스트 절차 (Testing Steps)

세팅을 마친 후 멀티플레이어 환경(Net Mode: Play as Client, Players: 2)에서 아래 절차를 통해 기능을 검증합니다.

1. **상호작용 및 카메라 시점 전환 테스트**:
   - 캐릭터로 조종하여 선박 위에 배치된 `BP_Cannon` 근처로 다가갑니다.
   - 상호작용 키(기본 F 키)를 누릅니다.
   - **결과**: 카메라 시점이 캐릭터 카메라에서 대포의 `AimCamera` 시점(1인칭 등)으로 즉시 부드럽게 전환되는지 확인합니다. 또한 화면에 크로스헤어 조준 UI가 생성되는지 확인합니다.

2. **조작 제한 및 마우스 조준 테스트**:
   - 조준 상태에서 W, A, S, D 키를 눌러봅니다.
   - **결과**: 캐릭터가 움직이지 않고 대포 위치에 완벽히 고정되는지 확인합니다.
   - 마우스를 좌우/상하로 움직여봅니다.
   - **결과**: 마우스 움직임에 따라 포신(`BarrelMesh`)과 포대(`BaseMesh`)가 부드럽게 회전하며, 설정한 제한 각도(Min/Max Pitch, Max Yaw Offset) 안에서만 클램프되어 작동하는지 확인합니다. 이 회전 움직임이 멀티플레이어 화면(상대 클라이언트 화면)에서도 동기화되어 보이는지 확인합니다.

3. **발사 및 쿨타임(광클 방지) 테스트**:
   - 마우스 좌클릭을 누릅니다.
   - **결과**: 포신 끝 방향으로 포물선을 그리며 `BP_Cannonball` 투사체가 날아가는지 확인합니다.
   - 마우스를 매우 빠르게 연속으로 광클해봅니다.
   - **결과**: 쿨타임(1.5초)이 돌고 있을 때는 대포알이 발사되지 않고, 쿨타임이 끝난 후에만 다음 대포알이 정상 발사되는지 확인합니다.

4. **선박 충돌 및 GAS 데미지 차감 테스트 (GAS 디버그)**:
   - 본인이 탑승 중인 배를 향해 대포알을 쏴봅니다.
   - **결과**: 본인의 배와는 오버랩 충돌이 일어나지 않고 대포알이 뚫고 지나가는지 확인합니다.
   - 다른 배(적 선박 혹은 다른 플레이어 배)를 향해 대포알을 쏴서 맞춥니다.
   - **결과**: 다른 배에 닿는 순간 충돌 지점에 붉은색 **Debug Sphere**가 그려지고, 대포알이 즉시 사라지는지 확인합니다.
   - **GAS 데미지 검증**:
     - 에디터 플레이 도중 콘솔창 키(`~` 또는 `@` 키)를 누르고 `showdebug abilitysystem` 명령어를 입력합니다.
     - 피격된 다른 배에 초점을 맞추거나 디버그 정보에서 해당 배의 Attribute 상태를 관찰합니다.
     - **결과**: 피격될 때마다 대상 배의 `Health` Attribute가 100 -> 90 -> 80 등으로 정확히 10씩 깎이는 것을 디버그 텍스트로 직접 확인합니다.

5. **물 파동 연동 및 N초 후 소멸 테스트**:
   - 대포알을 배에 맞추지 않고 물(WaterBody 등) 영역에 쏴봅니다.
   - **결과**: 대포알이 수면에 닿는 순간 물 파동(Ripple)이 링 형태로 정상 생성되는지 확인합니다.
   - 수면에 닿은 대포알은 비행을 멈추고 겉모습이 사라지며, 설정한 N초(Life Time After Water Hit, 기본 2초)가 지난 후 월드에서 완전히 삭제(Destroy)되는지 아웃라이너 등을 통해 확인합니다.

6. **조준 탈출 및 원복 테스트**:
   - 대포 조작 상태에서 조준 탈출 키(F 키)를 누릅니다.
   - **결과**: 카메라 시점이 원래 캐릭터 카메라로 복귀하고, 조준 UI가 화면에서 제거되며, 캐릭터의 이동 제약이 풀려 다시 자유롭게 돌아다닐 수 있는지 확인합니다.
